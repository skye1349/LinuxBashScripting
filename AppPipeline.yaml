name: AppPipeline

pool:
  vmImage: 'ubuntu-latest'

trigger: none

resources:
  repositories:
  - repository: Scripts
    type: git
    name: Scripts
    refs: refs/heads/main

extends:
  template: MainPipeline.yaml