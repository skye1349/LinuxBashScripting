name: AppPipeline

pool:
  vmImage: 'ubuntu-latest'

trigger: none

resources:
  repositories:
  - repository: Scripts
    type: github
    name: skye1349/Scripts
    refs: refs/heads/main
    endpoints: apps

extends:
  template: MainPipeline.yaml